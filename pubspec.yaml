name: syncfusion_charts
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  syncfusion_flutter_charts: ^25.1.40
    # git:
    #   url: https://github.com/btrautmann/syncfusion-flutter-widgets.git
    #   ref: bt/use-characters

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
